{% extends 'web/base.html.twig' %}

{% block title %}{{ 'Project' | trans }} · {{ parent() }}{% endblock %}

{% block body %}
    <div class="container py-5">
        <div class="row">
            <div class="col">
                <h1 class="pb-2 border-bottom mb-5"><span class="placeholder col-{{ random(8, 11) }}"></span></h1>
                <h4 class="text-secondary mb-5"><span class="placeholder col-{{ random(5, 12) }}"></span></h4>
                <p>
                    {% include 'helpers/random_paragraph_placeholder.html.twig' %}
                </p>
            </div>
        </div>
    </div>
{% endblock %}
