{% extends 'web/base.html.twig' %}

{% block title %}{{ project.name }} · {{ parent() }}{% endblock %}

{% block body %}
    <div class="container py-5">
        <div class="row">
            <div class="col">
                <h1 class="mb-0">{{ 'project' | trans | capitalize }} {{ project.name }}</h1>
            </div>
        </div>
    </div>
{% endblock %}
