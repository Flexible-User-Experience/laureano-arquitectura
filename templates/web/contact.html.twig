{% extends 'web/base.html.twig' %}

{% block title %}{{ 'contact' | trans | capitalize }} · {{ parent() }}{% endblock %}

{% block body %}
    <div class="container py-5">
        <div class="row">
            <div class="col">
                <h1>{{ 'contact' | trans | capitalize }}</h1>
                {{ form(form) }}
            </div>
            <div class="col">
                <h1>{{ 'map' | trans | capitalize }}</h1>
            </div>
        </div>
    </div>
{% endblock %}
